import { UserSearchfilterPipe } from './user-searchfilter.pipe';

describe('UserSearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UserSearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
