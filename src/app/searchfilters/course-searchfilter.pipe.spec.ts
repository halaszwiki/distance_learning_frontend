import { CourseSearchfilterPipe } from './course-searchfilter.pipe';

describe('CourseSearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseSearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
